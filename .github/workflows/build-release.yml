name: Build and release exe

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt pyinstaller

      - name: Run tests
        run: python -m unittest discover -s tests -v

      - name: Build app exe
        run: pyinstaller --onefile --noconsole --name OBSAutoRecorder --distpath dist --workpath build --specpath build autostart_script.py

      - name: Build installer exe
        # --add-data source paths are resolved relative to --specpath, not the working
        # directory, so they must be absolute here.
        run: pyinstaller --onefile --noconsole --uac-admin --name OBSAutoRecorderInstaller --distpath dist --workpath build --specpath build --add-data "${{ github.workspace }}\dist\OBSAutoRecorder.exe;." --add-data "${{ github.workspace }}\config.example.json;." installer.py

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: OBSAutoRecorder
          path: |
            dist/OBSAutoRecorder.exe
            dist/OBSAutoRecorderInstaller.exe

      - name: Create versioned GitHub release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release create "${{ github.ref_name }}" dist/OBSAutoRecorderInstaller.exe dist/OBSAutoRecorder.exe config.example.json --title "${{ github.ref_name }}" --generate-notes

      - name: Publish rolling "latest" build (main branch)
        if: github.ref == 'refs/heads/main'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          git tag -f latest
          git push origin latest --force
          gh release delete latest --yes || true
          gh release create latest dist/OBSAutoRecorderInstaller.exe dist/OBSAutoRecorder.exe config.example.json \
            --title "Latest build (main)" \
            --notes "Automatically built from the latest commit on main (${{ github.sha }}). May be less stable than a tagged release." \
            --prerelease
